spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Ag_flat_rent
    username: postgres
    password: Superp0stgrE
    driver: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

logging:
  level:
#    liquibase: DEBUG
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: info
